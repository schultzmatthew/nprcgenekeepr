#' Copyright(c) 2017-2020 R. Mark Sharp
# This file is part of nprcgenekeepr
# context("getRepresentativeParents")
# library(testthat)
# library(stringi)
# ped <- nprcgenekeepr::pedSix
# test_that(
#   stri_c("getPotentialParents returns the correct subsets of male ",
#          "and females"), {
#   parents <- getRepresentativeParents(id = "d2", ped)
#   expect_true(all(is.element(parents$sire, c("s1", "s2"))))
#   expect_true(all(is.element(parents$dam, c("d1"))))
# })
#
